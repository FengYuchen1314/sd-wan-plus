# PathWeaver Web Console

Vue 3 + TypeScript + Vite + Pinia + Cytoscape.js

```bash
npm install
npm run dev    # http://127.0.0.1:5173 (proxies API to :14301)
npm run build  # output: dist/ — served by Go controller via PW_STATIC_DIR
```
